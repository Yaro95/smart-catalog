body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  background-color: #f4f4f4;
}

header {
  background-color: #333;
  color: #fff;
  padding: 20px;
  text-align: center;
}

h1 {
  margin: 0;
}

main {
  padding: 20px;
}

section {
  margin-bottom: 20px;
}

.blocks, .floors, .apartments {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.block, .floor, .apartment {
  background-color: #fff;
  border: 1px solid #ccc;
  padding: 15px;
  text-align: center;
  cursor: pointer;
  transition: background-color 0.3s;
}

.block:hover, .floor:hover, .apartment:hover {
  background-color: #ddd;
}

footer {
  text-align: center;
  padding: 20px;
  background-color: #333;
  color: #fff;

}
#home-button {
  position: absolute;
  top: 20px;
  left: 20px;
  padding: 10px 15px;
  background-color: #333;
  color: white;
  text-decoration: none;
  border-radius: 5px;
  font-weight: bold;
  transition: background-color 0.3s;
}

#home-button:hover {
  background-color: #555;
}
